{
  "name": "agent-mail",
  "version": "0.2.0",
  "description": "Durable local mailbox + job scheduler for agent-to-agent coordination. Zero infra — SQLite files.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./scheduler": {
      "types": "./dist/scheduler.d.ts",
      "import": "./dist/scheduler.js"
    }
  },
  "bin": {
    "agent-mail": "./dist/cli.js",
    "agent-sched": "./dist/scheduler-cli.js",
    "agent-sched-daemon": "./dist/scheduler-daemon.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "agent-mail": "tsx src/cli.ts"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "croner": "^8.1.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  }
}
